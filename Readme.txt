scripts contents, description


-makeTranscript htmlFile lengthInSecondsOfWav           (takes html and creates a .stm file with estimated times for text lines.  using 30 words per line, while modeltraining uses 15words/line)


-Chop_wav_silence_pts wavFile chopped_audio_file_dir   (no / at end of dir)


-Chop_text_match_wav_2  (executed in spyder, with fileName, and timing points determined for each earn call specifically.  also converts chopped files to .mp3, splits data into four parts)



-Worker_file_read  (executed in spyder, using full_text.txt that was output from the mTurk worker, creates a .stm file from the text lines corresponding to each audio file)




